---
phase: 01-high-impact-ctr-and-content-depth
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - scripts/build.py
autonomous: true
requirements:
  - DEPTH-01
  - DEPTH-02

must_haves:
  truths:
    - "Vendor comparison page has a structured comparison table section with rows for each vendor across key dimensions"
    - "Vendor comparison page has FAQ schema markup with 3+ FAQs that match the visible FAQ section"
    - "DH alternative page has a pricing comparison section showing DH costs vs Provyx costs side-by-side"
    - "All new content sections render correctly in the built HTML"
  artifacts:
    - path: "scripts/build.py"
      provides: "New comparison table section and FAQ entries for vendor comparison page, new pricing comparison section for DH alternative page"
      contains: "pricing comparison HTML in DH alternative data dict and comparison table + FAQ entries in vendor comparison data dict"
    - path: "resources/healthcare-data-vendor-comparison/index.html"
      provides: "Built vendor comparison page with table and FAQ"
      contains: "FAQPage schema JSON-LD and comparison table HTML"
    - path: "alternatives/definitive-healthcare-alternative/index.html"
      provides: "Built DH alternative page with pricing comparison"
      contains: "pricing comparison section HTML"
  key_links:
    - from: "scripts/build.py"
      to: "resources/healthcare-data-vendor-comparison/index.html"
      via: "build_resource_page function"
      pattern: "build_resource_page"
    - from: "scripts/build.py"
      to: "alternatives/definitive-healthcare-alternative/index.html"
      via: "build_alternative_page function"
      pattern: "build_alternative_page"
---

<objective>
Add content depth to the vendor comparison and DH alternative pages: a structured comparison table + FAQ schema on the vendor comparison page, and a pricing comparison section on the DH alternative page.

Purpose: These pages rank (positions 6-8.3) but have near-zero CTR partly because they lack the structured content (tables, FAQs) that Google can surface as rich results. Adding comparison tables improves scannability for users who do click. FAQ schema gives Google FAQ-rich-result eligibility. Pricing comparison on the DH alternative page directly addresses the top query "definitive healthcare subscription cost."

Output: Updated data dicts in `scripts/build.py` with new sections and FAQ entries, plus rebuilt HTML pages.
</objective>

<execution_context>
@/Users/rome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-high-impact-ctr-and-content-depth/01-01-SUMMARY.md
@scripts/build.py
@scripts/templates.py
</context>

<interfaces>
<!-- Key functions the executor needs from templates.py -->
From scripts/templates.py:
- `generate_faq_html(faqs)` — takes list of {"question": str, "answer": str} dicts, returns FAQ section HTML + FAQPage schema JSON-LD
- `get_page_wrapper(...)` — wraps page content with head/nav/footer
- `write_page(...)` — writes HTML to disk

From scripts/build.py:
- Resource pages use `build_resource_page(res)` which reads `res["sections"]` list (each has "heading" and "body") and `res["faqs"]` list
- Alternative pages use `build_alternative_page(alt)` which reads section fields like `why_switch_body`, `hero_subtitle`, and custom sections
- The vendor comparison resource page data dict starts at line ~10886
- The DH alternative page data dict starts at line ~5014
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add comparison table and FAQ schema to vendor comparison page</name>
  <files>scripts/build.py</files>
  <action>
Modify the vendor comparison resource page data dict (slug: `healthcare-data-vendor-comparison`, starts at line ~10886 in RESOURCES list) in two ways:

**1. Add a comparison table section** to the `sections` list. Insert it as the second section (after "Why a Comparison Like This Is Overdue", before "The Enterprise Incumbents"). The section should have:
- Heading: "At-a-Glance: 8 Vendors Compared"
- Body: An HTML table with these columns: Vendor | Coverage | Pricing Model | Min. Commitment | Healthcare-Specific | Best For
- Rows for: IQVIA OneKey, Definitive Healthcare, ZoomInfo, Apollo.io, Cognism, Lusha, CarePrecise, Provyx
- Use data already present in the existing sections (enterprise incumbents section, generalist platforms section, etc.) -- do NOT fabricate data points
- Pricing data to use (already in the page content): IQVIA ($100K+/yr), Definitive ($30K-$50K+ single seat), ZoomInfo ($15K-$25K/yr), Apollo (free tier to $99-$149/mo), Cognism (custom, ~$15K-$30K/yr), Lusha (free to $79/mo), CarePrecise (pay-per-record), Provyx (pay-per-record)
- Coverage data to use: IQVIA (9M+ global HCPs), Definitive (hospitals/physicians/facilities), ZoomInfo (260M+ B2B contacts), Apollo (275M+ contacts), Cognism (400M+ B2B profiles), Lusha (100M+ contacts), CarePrecise (US providers), Provyx (US providers)
- Healthcare-specific column: Yes/No for each vendor
- Best For: one phrase per vendor (e.g., "Enterprise pharma", "Hospital intelligence", "General B2B + healthcare", etc.)
- Style the table with existing CSS classes from the site (use class="comparison-table" or similar existing pattern)

**2. Add/update the `faqs` list** for this page. The page may already have FAQs -- check. Add at least 5 FAQs total that match the top GSC queries. FAQ questions should target:
- "How do healthcare data vendors compare on pricing?" (addresses "healthcare commercial intelligence vendors comparison" query)
- "What is the best healthcare data vendor for small teams?" (addresses small-team searchers)
- "Which healthcare data vendors include NPI verification?" (addresses accuracy/quality queries)
- "How accurate is healthcare provider data from commercial vendors?" (addresses firmographic data provider queries)
- "What healthcare-specific features should I look for in a data vendor?" (addresses comparison queries)
- Answers: 2-4 sentences each, factual, reference data from the comparison table above. Mention specific vendors and price points already stated in the article.

Do NOT change the title or meta_description (those were updated in Plan 01).
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/provyx-website && python3 -c "
import sys; sys.path.insert(0, 'scripts')
from build import RESOURCES
vc = [r for r in RESOURCES if r['slug']=='healthcare-data-vendor-comparison'][0]
# Check comparison table section exists
table_sections = [s for s in vc['sections'] if 'table' in s['body'].lower() or '<table' in s['body'].lower()]
assert len(table_sections) >= 1, 'No comparison table section found'
assert '<table' in table_sections[0]['body'].lower(), 'Section does not contain HTML table'
# Check FAQs
assert 'faqs' in vc, 'No faqs key in vendor comparison data'
assert len(vc['faqs']) >= 5, f'Only {len(vc[\"faqs\"])} FAQs, need 5+'
print(f'Vendor comparison: table section found, {len(vc[\"faqs\"])} FAQs. PASS')
"
    </automated>
  </verify>
  <done>Vendor comparison page has a structured at-a-glance comparison table with all 8 vendors and 5+ FAQ entries with FAQPage schema markup.</done>
</task>

<task type="auto">
  <name>Task 2: Add pricing comparison section to DH alternative page</name>
  <files>scripts/build.py</files>
  <action>
Modify the DH alternative page data dict (slug: `definitive-healthcare-alternative`, starts at line ~5014 in ALTERNATIVES list) to add a dedicated pricing comparison section.

The alternative page template uses these section fields (check `build_alternative_page` function for exact field names):
- `why_switch_heading` / `why_switch_body` (already populated)
- Look for a `comparison_sections` or similar list, OR check if the template supports additional custom sections

**Add a pricing comparison section** that includes:
- A clear heading like "What Definitive Healthcare Costs vs. Provyx"
- A side-by-side HTML table or structured comparison:
  | | Definitive Healthcare | Provyx |
  |---|---|---|
  | Entry price | $25,000-$50,000/yr | Pay-per-record (no minimum) |
  | Full platform | $75,000-$100,000+/yr | Pay-per-record |
  | CRM integration | $22,000+/yr add-on | Included (CSV/API) |
  | Contract | Annual, auto-renew with 5% uplift | No annual contract |
  | Per-seat licensing | Yes (per user) | No per-seat fees |
  | Data export | Platform-dependent | Full export included |
- All pricing data must come from what is ALREADY stated in the `why_switch_body` text on this page (DH $25K-$50K entry, $75K-$100K+ full, $22K CRM add-on, 5% auto-renew uplift). Do not fabricate new numbers.
- A paragraph below the table explaining when DH is worth it (hospital/IDN intelligence, claims data, referral networks) vs when Provyx is the better fit (outbound sales teams needing provider contacts without analytics platform)
- Keep the honest positioning per CLAUDE.md writing rules -- acknowledge DH strengths

**Implementation approach:** Check how `build_alternative_page()` renders content. If it uses a `sections` list like resource pages, add the pricing section there. If it uses individual named fields, add the pricing HTML into the `why_switch_body` field as a new subsection, or find the appropriate field for additional content sections.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/provyx-website && python3 -c "
import sys; sys.path.insert(0, 'scripts')
from build import ALTERNATIVES
dha = [a for a in ALTERNATIVES if a['slug']=='definitive-healthcare-alternative'][0]
# Check for pricing table content somewhere in the data dict values
all_text = str(dha)
assert 'table' in all_text.lower() or '<table' in all_text.lower(), 'No pricing table found in DH alternative data'
assert '25,000' in all_text or '25000' in all_text, 'DH pricing not mentioned in comparison'
assert 'pay-per-record' in all_text.lower() or 'Pay-per-record' in all_text, 'Provyx pricing model not mentioned'
print('DH alternative: pricing comparison section with table found. PASS')
"
    </automated>
  </verify>
  <done>DH alternative page has a pricing comparison section with side-by-side cost table showing DH vs Provyx pricing, using only data points already present in the page content.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild site and verify both pages</name>
  <files>scripts/build.py</files>
  <action>
Run `python3 scripts/build.py` from project root to regenerate all HTML pages. Then verify:

1. `resources/healthcare-data-vendor-comparison/index.html` contains:
   - An HTML `<table` element with vendor comparison data
   - FAQPage schema JSON-LD block (`"@type": "FAQPage"`)
   - At least 5 FAQ entries visible in the HTML

2. `alternatives/definitive-healthcare-alternative/index.html` contains:
   - An HTML `<table` element with pricing comparison data
   - Both "25,000" and "pay-per-record" in the page content

3. Both pages pass the SEO checklist from CLAUDE.md:
   - Title 50-60 chars (set in Plan 01)
   - Meta 120-158 chars (set in Plan 01)
   - BreadcrumbList schema present
   - 2+ outbound links to authoritative sources
   - H1 present
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/provyx-website && python3 scripts/build.py 2>&1 | tail -3 && echo "---VENDOR COMPARISON---" && grep -c '<table' resources/healthcare-data-vendor-comparison/index.html && grep -c 'FAQPage' resources/healthcare-data-vendor-comparison/index.html && echo "---DH ALTERNATIVE---" && grep -c '<table' alternatives/definitive-healthcare-alternative/index.html && grep -c 'pay-per-record' alternatives/definitive-healthcare-alternative/index.html</automated>
  </verify>
  <done>Build completes. Vendor comparison page has comparison table + FAQPage schema. DH alternative page has pricing comparison table. Both pages meet SEO checklist requirements.</done>
</task>

</tasks>

<verification>
- Vendor comparison page has at-a-glance table with 8 vendors
- Vendor comparison page has FAQPage schema with 5+ FAQs
- DH alternative page has pricing comparison section with cost table
- Build runs without errors
- All content uses only data points already present in the articles (no fabricated stats)
</verification>

<success_criteria>
Both pages have deeper, more structured content that improves scannability (comparison tables), FAQ rich-result eligibility (FAQPage schema), and directly addresses high-intent search queries about pricing and vendor comparisons.
</success_criteria>

<output>
After completion, create `.planning/phases/01-high-impact-ctr-and-content-depth/01-02-SUMMARY.md`
</output>
