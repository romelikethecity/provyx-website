---
phase: 02-remaining-ctr-and-service-page-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build.py
autonomous: true
requirements: [CTR-05, CTR-06, CTR-07, CTR-08, CTR-09, CTR-10]

must_haves:
  truths:
    - "All 6 CTR pages have keyword-first titles under 60 chars matching their top GSC queries"
    - "All 6 CTR pages have meta descriptions 120-158 chars leading with what the reader gets"
    - "H1 tags are updated to match new titles where they differ"
    - "Site builds successfully with all pages reflecting updated metas"
  artifacts:
    - path: "scripts/build.py"
      provides: "Updated title, meta_description, and h1 fields for 6 pages"
    - path: "services/practice-location-data/index.html"
      provides: "Rebuilt page with new title/meta"
    - path: "resources/healthcare-data-providers-small-teams/index.html"
      provides: "Rebuilt page with new title/meta"
    - path: "compare/provyx-vs-lusha/index.html"
      provides: "Rebuilt page with new title/meta"
    - path: "providers/oral-pathologists/index.html"
      provides: "Rebuilt page with new title/meta"
    - path: "resources/nppes-vs-commercial-provider-data/index.html"
      provides: "Rebuilt page with new title/meta"
    - path: "alternatives/zoominfo-alternative/index.html"
      provides: "Rebuilt page with new title/meta"
  key_links:
    - from: "scripts/build.py"
      to: "HTML output files"
      via: "python3 scripts/build.py"
      pattern: "title.*meta_description"
---

<objective>
Rewrite title tags, meta descriptions, and H1 tags on the 6 remaining CTR pages (100+ impressions each) to match their top GSC search queries and follow the keyword-first, outcome-oriented pattern established in Phase 1.

Purpose: These 6 pages have 935 combined impressions but poor CTR because titles and metas do not match searcher intent. Aligning them with actual GSC queries earns more clicks from existing rankings.

Output: Updated title/meta/h1 fields in build.py data dicts + rebuilt HTML pages.
</objective>

<execution_context>
@/Users/rome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-high-impact-ctr-and-content-depth/01-01-SUMMARY.md
@scripts/build.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite titles, metas, and H1s for 6 CTR pages</name>
  <files>scripts/build.py</files>
  <action>
Rewrite title, meta_description, and h1/page_title/hero_h1/hero_headline fields for each page in build.py. Follow the Phase 1 pattern: titles are keyword-first, 50-60 chars, outcome-oriented. Metas are 120-158 chars, lead with what the reader gets.

Use these GSC queries to guide rewrites:

1. `/services/practice-location-data/` (189 impr, pos 10.3)
   - Top queries: "practice location", "medical practice location system"
   - Current title: "Geocoded Healthcare Practice Location Intelligence"
   - Rewrite title to lead with "Practice Location Data" (the exact query), add outcome hook. ~55 chars.
   - Rewrite meta to emphasize geocoded addresses, multi-site mapping, territory planning use case. 120-158 chars.
   - Field path: SERVICES list, slug "practice-location-data", fields: title, meta_description, subtitle (serves as H1 area)

2. `/resources/healthcare-data-providers-small-teams/` (180 impr, pos 16.2)
   - Top queries: "healthcare data providers small teams", "b2b healthcare data providers"
   - Current title: "Best Healthcare Data Providers for Small Sales Teams"
   - Rewrite title to lead with "Healthcare Data Providers" keyword, keep "Small Teams" angle. ~55 chars.
   - Rewrite meta to lead with what small teams get (per-record pricing, no contracts). 120-158 chars.
   - Field path: RESOURCES list, slug "healthcare-data-providers-small-teams", fields: title, meta_description, h1

3. `/compare/provyx-vs-lusha/` (162 impr, pos 7.6)
   - Top queries: "lusha medical practice management"
   - Current title: "Provyx vs. Lusha: Healthcare Provider Data Compared"
   - Current page_title: same
   - Rewrite to lead with "Provyx vs Lusha" (drop the period in "vs."), add healthcare data hook. ~55 chars.
   - Rewrite meta to address why healthcare teams evaluate Lusha and what they find. 120-158 chars.
   - Field path: COMPARISONS list, slug "provyx-vs-lusha", fields: page_title, meta_description, hero_headline

4. `/providers/oral-pathologists/` (143 impr, pos 15.8)
   - Top queries: "oral pathologists email list"
   - This is a programmatic provider subtype page. Title is generated from the tuple format.
   - The tuple at line ~235 defines: ("oral-pathologists", "Oral Pathologists", "dental", [], "Oral pathologist data for providers specializing in diagnosis of oral and maxillofacial diseases.")
   - The 5th element is the meta_description. Rewrite this meta to lead with "oral pathologist email list" angle (matching the query) and mention verified contacts, practice data. 120-158 chars.
   - For title: Provider subtype pages use a generated title pattern. Check how build_provider_subtype_page() constructs the title tag. If the title is generated like "Oral Pathologists - Provider Data | Provyx", it may need a custom override OR the generation pattern may already be good. If the title is already keyword-appropriate, focus on meta only.

5. `/resources/nppes-vs-commercial-provider-data/` (143 impr, pos 20.8)
   - Top queries: "nppes database accuracy", "nppes data"
   - Current title: "NPPES Data Download vs Commercial Provider Data: A Practical Comparison"
   - Rewrite title to lead with "NPPES" keyword more prominently, add accuracy angle (matches top query). ~55 chars.
   - Rewrite meta to lead with what reader learns (field-by-field comparison, accuracy gaps, when each makes sense). 120-158 chars.
   - Field path: RESOURCES list, slug "nppes-vs-commercial-provider-data", fields: title, meta_description, h1

6. `/alternatives/zoominfo-alternative/` (118 impr, pos 6.1)
   - Top queries: "zoominfo healthcare alternative"
   - Current title: "Best ZoomInfo Alternative for Healthcare Data"
   - Rewrite title to lead with "ZoomInfo Alternative" keyword, add healthcare provider data hook. ~55 chars.
   - Rewrite meta to lead with what healthcare teams get by switching (NPI-verified data, pay-per-record, no contract). 120-158 chars.
   - Field path: ALTERNATIVES list, slug "zoominfo-alternative", fields: title, meta_description, hero_h1

For each page, also update the H1 field (h1, hero_h1, hero_headline, or page_title depending on page type) to match or closely align with the new title. Phase 1 established this pattern.

Do NOT change any content body, FAQ, or section content. Only title, meta, and H1-level fields.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/provyx-website && python3 -c "
import subprocess, sys
result = subprocess.run(['python3', 'scripts/build.py'], capture_output=True, text=True)
if result.returncode != 0:
    print('BUILD FAILED:', result.stderr[:500])
    sys.exit(1)
print('Build succeeded')
# Check each page exists and has updated title tag
import os
pages = [
    'services/practice-location-data/index.html',
    'resources/healthcare-data-providers-small-teams/index.html',
    'compare/provyx-vs-lusha/index.html',
    'providers/oral-pathologists/index.html',
    'resources/nppes-vs-commercial-provider-data/index.html',
    'alternatives/zoominfo-alternative/index.html',
]
for p in pages:
    if not os.path.exists(p):
        print(f'MISSING: {p}')
        sys.exit(1)
    content = open(p).read()
    # Verify title tag exists and is under 70 chars (with site name suffix)
    import re
    title_match = re.search(r'<title>(.*?)</title>', content)
    if title_match:
        t = title_match.group(1)
        print(f'OK: {p} -> title={len(t)} chars: {t[:70]}')
    else:
        print(f'NO TITLE: {p}')
        sys.exit(1)
print('All 6 pages verified')
"
    </automated>
  </verify>
  <done>All 6 pages have rewritten keyword-first titles (50-60 chars), metas (120-158 chars), and aligned H1 tags. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
- Build completes without errors
- All 6 HTML pages exist with updated title tags
- Title tags are 50-60 chars, keyword-first
- Meta descriptions are 120-158 chars
- H1 tags match or align with titles
</verification>

<success_criteria>
All 6 remaining CTR pages have titles and metas rewritten to match their top GSC queries, following the keyword-first pattern from Phase 1. Site builds and all pages render correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-remaining-ctr-and-service-page-depth/02-01-SUMMARY.md`
</output>
