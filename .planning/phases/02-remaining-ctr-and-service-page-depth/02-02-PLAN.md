---
phase: 02-remaining-ctr-and-service-page-depth
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - scripts/build.py
autonomous: true
requirements: [DEPTH-04, DEPTH-05]

must_haves:
  truths:
    - "Technology detection page shows specific EHR install base examples with sales workflow applications"
    - "Practice firmographics service page has expanded data field descriptions and use-case examples"
    - "Healthcare provider firmographic data resource page has expanded content with practical field descriptions"
    - "All three pages meet the 1,200+ word minimum for service/resource pages"
  artifacts:
    - path: "scripts/build.py"
      provides: "Expanded content sections for technology-detection, practice-firmographics, and healthcare-provider-firmographic-data"
    - path: "services/technology-detection/index.html"
      provides: "Rebuilt page with EHR install base examples"
    - path: "services/practice-firmographics/index.html"
      provides: "Rebuilt page with expanded firmographic content"
    - path: "resources/healthcare-provider-firmographic-data/index.html"
      provides: "Rebuilt page with expanded firmographic content"
  key_links:
    - from: "services/technology-detection/index.html"
      to: "/services/practice-firmographics/"
      via: "internal link in content"
      pattern: "href.*practice-firmographics"
    - from: "services/practice-firmographics/index.html"
      to: "/resources/healthcare-provider-firmographic-data/"
      via: "internal link in content"
      pattern: "href.*firmographic-data"
    - from: "resources/healthcare-provider-firmographic-data/index.html"
      to: "/services/practice-firmographics/"
      via: "internal link in content"
      pattern: "href.*practice-firmographics"
---

<objective>
Add EHR install base examples and sales workflow applications to the technology detection service page, and expand firmographic content on both the service page and resource page with specific data field descriptions and use-case examples.

Purpose: These pages rank for high-intent queries ("ehr lead generation", "healthcare software abm", "healthcare firmographic data providers") but lack the content depth to convert searchers. Adding concrete examples and field-level documentation signals expertise and matches informational search intent.

Output: Expanded content sections in build.py + rebuilt HTML pages with deeper content.
</objective>

<execution_context>
@/Users/rome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-remaining-ctr-and-service-page-depth/02-01-SUMMARY.md
@scripts/build.py

<interfaces>
<!-- Service page structure in build.py SERVICES list -->
<!-- Each service dict has: slug, title, short_title, aeo_definition, aeo_steps, subtitle, meta_description, outbound_links, problem_heading, problem_body, included_heading, included_features (list of {name, icon, description}), faqs, related_services, related_content, related_provider_categories, testimonial -->
<!-- Resource page structure in build.py RESOURCES list -->
<!-- Each resource dict has: slug, title, meta_description, h1, subtitle, sections (list of {heading, body}), faqs, related_content, author -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EHR install base examples to technology detection page</name>
  <files>scripts/build.py</files>
  <action>
Add a new content section to the technology detection service page (slug: "technology-detection" in SERVICES list) that shows concrete EHR install base examples applied to sales workflows. The top GSC queries are "ehr lead generation" and "healthcare software abm" -- content must directly address these use cases.

Add a new included_feature entry (or expand existing ones) with content covering:

1. **EHR Install Base for Sales Targeting** -- A new included_feature showing how technology detection data translates to sales action. Include specific examples:
   - "A medical device company targeting practices on eClinicalWorks can filter 4,200+ practices nationally using that EHR, then layer on specialty and practice size to build a segment of 800 orthopedic practices with 3+ providers -- ready for outreach."
   - "A patient engagement startup identifies 1,200 dermatology practices still using on-premise EHR systems with no telehealth integration. These practices are displacement targets for cloud-based solutions."
   - "A billing company spots 600 multi-location pain management groups using legacy RCM software from a vendor acquired last year. Contract uncertainty creates a 6-month window for competitive pitches."

2. **Healthcare Software ABM Workflows** -- Another included_feature or expansion showing the ABM workflow:
   - Step-by-step: identify accounts by EHR type -> segment by practice size and specialty -> score by replacement cycle signals (contract age, vendor instability, regulatory deadlines) -> route to reps with technology context
   - Explain how knowing the installed EHR turns a cold call into "I see you're running [system] -- we integrate natively with that" or "practices switching from [legacy system] typically save X hours/week"

Keep the same writing style as existing content: specific, practical, no buzzwords. Use the "what this means for your sales team" framing. Reference ONC Health IT (already in outbound_links) for credibility.

Do NOT modify existing included_features -- add new ones after the existing list. Each new feature needs name, icon, and description fields matching the existing pattern.

Suggested icons: "database" for install base, "target" for ABM workflows (check what icons are used across other service pages for consistency).
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/provyx-website && python3 -c "
import subprocess, sys
result = subprocess.run(['python3', 'scripts/build.py'], capture_output=True, text=True)
if result.returncode != 0:
    print('BUILD FAILED:', result.stderr[:500])
    sys.exit(1)
content = open('services/technology-detection/index.html').read()
checks = ['EHR', 'install base', 'eClinicalWorks', 'ABM', 'practice size']
for c in checks:
    if c.lower() not in content.lower():
        print(f'MISSING content: {c}')
        sys.exit(1)
    print(f'FOUND: {c}')
# Word count check
import re
text = re.sub(r'<[^>]+>', ' ', content)
words = len(text.split())
print(f'Word count: {words}')
if words < 1200:
    print('WARNING: Under 1200 words')
print('Technology detection page verified')
"
    </automated>
  </verify>
  <done>Technology detection page includes EHR install base examples with specific practice counts and sales workflow applications. ABM workflow section shows step-by-step process. Page meets 1,200+ word minimum.</done>
</task>

<task type="auto">
  <name>Task 2: Expand firmographic content on service and resource pages</name>
  <files>scripts/build.py</files>
  <action>
Expand content on both firmographic pages to add specific data field descriptions and use-case examples. The top GSC queries are "healthcare firmographic data providers" and "healthcare firmographic data" -- content must directly address what firmographic data includes and how it is used.

**Page 1: /services/practice-firmographics/ (slug: "practice-firmographics" in SERVICES list)**

Add 1-2 new included_features covering:

1. **Data Fields Reference** -- A detailed included_feature listing every firmographic field Provyx provides with descriptions:
   - Provider headcount (total, by credential type: MD/DO, NP, PA, other)
   - Revenue range estimate (methodology: specialty benchmarks x provider count x geo adjustment)
   - Ownership classification (solo, group, DSO/MSO-affiliated, health system-owned, PE-backed)
   - Years in operation (from NPI enumeration date + state business filing)
   - Multi-location flag and location count
   - Practice type (single-specialty, multi-specialty, hospital-based)
   This is the "data field documentation" that searches for "firmographic data" expect to find.

2. **Segmentation Examples** -- Concrete filtering examples:
   - "Filter to independent dental practices with 3-8 providers and $2M-$5M estimated revenue -- your mid-market sweet spot for practice management software"
   - "Identify PE-backed dermatology groups with 10+ locations for enterprise platform pitches"
   - "Exclude health system-owned practices where purchasing decisions happen at the corporate level"

**Page 2: /resources/healthcare-provider-firmographic-data/ (slug: "healthcare-provider-firmographic-data" in RESOURCES list)**

Add a new section (or expand the "Healthcare-Specific Firmographic Fields That Matter" section) with:

1. **Field-by-field reference table** -- Add an HTML table within a section body showing: Field Name | What It Tells You | Source Methodology | Sales Application. Cover 8-10 fields (provider count, revenue range, ownership type, years in operation, specialty mix, location count, technology stack, payer mix indicators).

2. **Use-case examples with specific scenarios** -- Expand the existing "Use Cases" section with 2-3 more concrete examples:
   - Medical device territory planning: "Assign territories by total provider count and practice revenue, not just zip codes. A territory with 200 solo practices has different value than one with 20 multi-provider groups."
   - SaaS product-market fit validation: "Filter your TAM to practices matching your ICP (e.g., 5-20 providers, independent ownership, using legacy EHR) to size the actual addressable market."
   - Private equity due diligence: "Map practice ownership structures across a specialty to identify consolidation patterns and remaining independent targets."

Ensure both pages cross-link to each other. The service page should link to the resource page as "for a deeper guide" and the resource page should link to the service page as "get practice firmographic data."

Keep writing style consistent: specific numbers, practical framing, no jargon. Reference BLS.gov (already in outbound_links on the service page) and CMS NPI Registry where appropriate.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/provyx-website && python3 -c "
import subprocess, sys
result = subprocess.run(['python3', 'scripts/build.py'], capture_output=True, text=True)
if result.returncode != 0:
    print('BUILD FAILED:', result.stderr[:500])
    sys.exit(1)
import re
# Check service page
svc = open('services/practice-firmographics/index.html').read()
svc_text = re.sub(r'<[^>]+>', ' ', svc)
svc_words = len(svc_text.split())
print(f'Firmographics service page: {svc_words} words')

# Check resource page
res = open('resources/healthcare-provider-firmographic-data/index.html').read()
res_text = re.sub(r'<[^>]+>', ' ', res)
res_words = len(res_text.split())
print(f'Firmographics resource page: {res_words} words')

# Check for new content
for label, content in [('service', svc), ('resource', res)]:
    checks = ['provider headcount', 'revenue', 'ownership']
    for c in checks:
        if c.lower() not in content.lower():
            print(f'MISSING in {label}: {c}')
            sys.exit(1)
        print(f'FOUND in {label}: {c}')

# Check cross-links
if '/resources/healthcare-provider-firmographic-data/' not in svc:
    print('MISSING: service->resource cross-link')
    sys.exit(1)
if '/services/practice-firmographics/' not in res:
    print('MISSING: resource->service cross-link')
    sys.exit(1)
print('Cross-links verified')
print('Both firmographic pages verified')
"
    </automated>
  </verify>
  <done>Practice firmographics service page has detailed data field reference and segmentation examples. Healthcare provider firmographic data resource page has field-by-field reference table and expanded use-case examples. Both pages cross-link to each other. Both meet word count minimums.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild site and verify all updated pages</name>
  <files>scripts/build.py</files>
  <action>
Run python3 scripts/build.py to rebuild all pages. Verify that:
1. Build completes without errors
2. All 3 depth pages (technology-detection, practice-firmographics, healthcare-provider-firmographic-data) have expanded content
3. All pages from Plan 01 CTR rewrites are still intact (not broken by content changes)
4. Sitemap includes all pages
5. No placeholder text or broken HTML in output

This is the final verification step for the entire phase.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/provyx-website && python3 scripts/build.py && echo "BUILD OK" && python3 -c "
import os, re, sys
pages = {
    'services/technology-detection/index.html': 1200,
    'services/practice-firmographics/index.html': 1200,
    'resources/healthcare-provider-firmographic-data/index.html': 1200,
    'services/practice-location-data/index.html': 0,
    'resources/healthcare-data-providers-small-teams/index.html': 0,
    'compare/provyx-vs-lusha/index.html': 0,
    'providers/oral-pathologists/index.html': 0,
    'resources/nppes-vs-commercial-provider-data/index.html': 0,
    'alternatives/zoominfo-alternative/index.html': 0,
}
for path, min_words in pages.items():
    if not os.path.exists(path):
        print(f'MISSING: {path}')
        sys.exit(1)
    content = open(path).read()
    if min_words > 0:
        text = re.sub(r'<[^>]+>', ' ', content)
        wc = len(text.split())
        status = 'OK' if wc >= min_words else 'LOW'
        print(f'{status}: {path} = {wc} words (min {min_words})')
        if wc < min_words:
            sys.exit(1)
    else:
        title = re.search(r'<title>(.*?)</title>', content)
        print(f'OK: {path} title={title.group(1)[:60] if title else \"NONE\"}')
# Check sitemap
if os.path.exists('sitemap.xml'):
    sm = open('sitemap.xml').read()
    for path in pages:
        slug = path.replace('/index.html', '/')
        if slug not in sm:
            print(f'NOT IN SITEMAP: {slug}')
print('All pages verified')
"
    </automated>
  </verify>
  <done>All 9 Phase 2 pages (6 CTR + 3 depth) are built, verified, and in the sitemap. No build errors, no broken pages.</done>
</task>

</tasks>

<verification>
- Build completes without errors
- Technology detection page has EHR install base examples and ABM workflow content
- Both firmographic pages have expanded data field descriptions and use-case examples
- Cross-links exist between firmographic service and resource pages
- All pages meet word count minimums (1,200+ for service/resource pages)
- All 6 CTR pages from Plan 01 remain intact
</verification>

<success_criteria>
Technology detection page directly addresses "ehr lead generation" and "healthcare software abm" queries with concrete examples. Both firmographic pages directly address "healthcare firmographic data" queries with field-level documentation and practical use cases. All 3 pages meet 1,200+ word minimum.
</success_criteria>

<output>
After completion, create `.planning/phases/02-remaining-ctr-and-service-page-depth/02-02-SUMMARY.md`
</output>
