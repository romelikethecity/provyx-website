---
phase: 04-content-gap-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build.py
autonomous: true
requirements: [GAP-05]

must_haves:
  truths:
    - "EHR lead generation use-case page exists at /use-cases/ehr-lead-generation/ with content connecting EHR install base data to sales prospecting"
  artifacts:
    - path: "scripts/build.py"
      provides: "New use-case data dict appended to USE_CASES list"
      contains: "ehr-lead-generation"
    - path: "use-cases/ehr-lead-generation/index.html"
      provides: "EHR lead generation use-case page"
      min_lines: 100
  key_links:
    - from: "scripts/build.py"
      to: "use-cases/ehr-lead-generation/index.html"
      via: "build_use_case_page() in build loop"
      pattern: "ehr-lead-generation"
---

<objective>
Create an EHR lead generation use-case page at /use-cases/ehr-lead-generation/ (GAP-05, 51+ impressions) following the existing use-case template pattern.

Purpose: Capture searchers looking for EHR-based lead generation strategies. This is the highest-impression gap query (51+) and connects directly to Provyx's technology detection service.
Output: New use-case data dict in build.py USE_CASES list, built and in sitemap.
</objective>

<execution_context>
@/Users/rome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/build.py
</context>

<interfaces>
<!-- Use-case data dict schema (from USE_CASES in build.py, consumed by build_use_case_page()): -->
```python
{
    "slug": str,                    # URL: /use-cases/{slug}/
    "title": str,                   # 50-60 chars
    "meta_description": str,        # 120-158 chars
    "h1": str,                      # 46-60 chars
    "subtitle": str,                # Hero subtitle paragraph
    "problem_heading": str,         # H2 for problem section
    "problem_content": str,         # HTML paragraphs describing the problem
    "solution_heading": str,        # H2 for solution section
    "solution_content": str,        # HTML paragraphs describing Provyx solution
    "how_it_works_heading": str,    # "How It Works"
    "how_it_works_steps": [         # 3-4 numbered steps
        {"title": str, "description": str},
    ],
    "results_heading": str,         # H2 for results section
    "results_content": str,         # HTML paragraphs on outcomes
    "faqs": [{"question": str, "answer": str}],  # Min 3
    "related_links": [{"url": str, "text": str}],  # 3-4 internal
    "outbound_links": [(url, text)],  # 2+ authoritative
}
```

<!-- Existing related use-case for reference: -->
From USE_CASES, "ehr-install-base-targeting" already exists with slug "ehr-install-base-targeting".
The new page should complement, not duplicate, that existing page.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add EHR lead generation use-case data dict to USE_CASES in build.py</name>
  <files>scripts/build.py</files>
  <action>
Append one new use-case data dict to the USE_CASES list in scripts/build.py. Follow the exact schema used by existing use-case entries (see interfaces block).

**EHR Lead Generation Use Case (GAP-05)**
- slug: "ehr-lead-generation"
- title: Under 60 chars, keyword "EHR lead generation" first
- h1: 46-60 chars, focused on EHR lead generation
- Target query intent: People searching "EHR lead generation", "electronic health record leads", "EHR sales leads", "how to sell to EHR users"
- Differentiation from existing "ehr-install-base-targeting" page: That page focuses on competitive displacement (targeting users of specific EHR systems). This page focuses on the broader lead generation workflow: how to identify, qualify, and reach practices based on their EHR adoption status for any sales motion (not just competitive replacement).

**problem_content must cover:**
- Why EHR data matters for lead gen: practices using specific EHRs have predictable tech budgets, integration needs, and pain points
- The challenge: EHR adoption data is fragmented across CMS attestation records, vendor press releases, and practice websites. No single public source gives you a complete view.
- Cost of guessing: reps waste time pitching integrations that don't match the prospect's current EHR, or miss practices that recently switched systems

**solution_content must cover:**
- How Provyx technology detection identifies EHR systems at the practice level (website analysis, attestation records, commercial databases)
- Filtering by EHR vendor + specialty + geography to build targeted lists
- Example workflows: health IT vendor targeting Epic practices in cardiology, medical device company finding practices on specific PM systems, consultant identifying practices still on legacy systems ready to migrate

**how_it_works_steps: 4 steps** covering: define target EHR systems, we identify practices using those systems, deliver verified contact list with EHR data appended, integrate into sales workflow

**results_content:** How teams using EHR-aware prospecting see better conversion because their pitch is relevant to the prospect's tech stack

**faqs:** Min 3, targeting EHR lead gen questions (what EHR systems can you identify, how current is the data, can I filter by specialty)

**related_links:** 3-4 internal links including /use-cases/ehr-install-base-targeting/, /services/technology-detection/, /use-cases/healthcare-sales-prospecting/, /for/health-it-companies/ (if exists)

**outbound_links:** 2+ including ONC Health IT Dashboard (https://www.healthit.gov/), CMS NPI Registry

Follow CLAUDE.md writing style. Total content should be 1,200-1,500 words across all sections combined.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/provyx-website && python3 -c "import sys; sys.path.insert(0,'scripts'); exec(open('scripts/build.py').read().split('# =====')[0])" 2>/dev/null; cd /Users/rome/Documents/projects/provyx-website && grep -c "ehr-lead-generation" scripts/build.py && echo "PASS: ehr-lead-generation slug found in build.py"</automated>
  </verify>
  <done>build.py USE_CASES list contains new ehr-lead-generation data dict with all required fields (slug, title, meta_description, h1, subtitle, problem_heading, problem_content, solution_heading, solution_content, how_it_works_heading, how_it_works_steps, results_heading, results_content, faqs, related_links, outbound_links)</done>
</task>

<task type="auto">
  <name>Task 2: Build site and verify EHR lead generation use-case page renders correctly</name>
  <files>use-cases/ehr-lead-generation/index.html, use-cases/index.html, sitemap.xml</files>
  <action>
Run `python3 scripts/build.py` from the project root directory. Then verify:
1. New use-case page HTML file exists at use-cases/ehr-lead-generation/index.html
2. URL appears in sitemap.xml
3. Use-cases index page lists the new page
4. Page has: title tag under 60 chars, meta description 120-158 chars, BreadcrumbList schema, canonical URL with getprovyx.com domain, OG + Twitter card tags
5. Page has 2+ outbound links to authoritative sources
6. Page has related links section with 3+ internal links
7. Page has FAQ section with FAQPage schema

If any meta description exceeds 158 chars or title exceeds 60 chars, fix in build.py and rebuild.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/provyx-website && python3 scripts/build.py 2>&1 | tail -5 && test -f use-cases/ehr-lead-generation/index.html && grep -c "ehr-lead-generation" sitemap.xml && echo "PASS: EHR lead gen use-case page built and in sitemap"</automated>
  </verify>
  <done>EHR lead generation use-case page renders as full HTML with proper SEO markup, appears in sitemap.xml and use-cases index</done>
</task>

</tasks>

<verification>
- `python3 scripts/build.py` completes without errors
- /use-cases/ehr-lead-generation/index.html exists with 1,200+ words
- URL in sitemap.xml
- Page passes SEO checklist (title, meta, schema, outbound links, related links)
- Content is differentiated from existing /use-cases/ehr-install-base-targeting/ page
</verification>

<success_criteria>
- EHR lead generation use-case page live at /use-cases/ehr-lead-generation/
- Page follows existing use-case template pattern exactly
- Proper schema markup (Breadcrumb + FAQ)
- In sitemap and use-cases index
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-gap-pages/04-03-SUMMARY.md`
</output>
